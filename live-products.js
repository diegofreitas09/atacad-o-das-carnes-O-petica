(function(){
  const oldNormalize=normalizeProduct,oldFetch=fetchFreshProducts,oldRender=renderProducts,oldRefresh=refreshProducts;
  const DELIVERY_CATEGORY='__ENTREGA__';
  const DEFAULT_DELIVERY_AREAS=[['Montese',15],['Parangaba',10],['Maraponga',10],['Pici',10],['Bela Vista',8],['Demócrito Rocha',8],['Henrique Jorge',8],['Vila Pery',6],['Manoel Sátiro',6],['Jatobá',6],['Pq. São José',6],['Pq. Santa Rosa',6],['Canindezinho',6],['Mondubim',7],['Conjunto Esperança',7],['Conjunto Ceará',5],['João XXIII',6],['Granja Lisboa',4],['Bom Sucesso',6],['Granja Portugal',4],['Bom Jardim',4],['Siqueira',6],['Benfica',20],['Centro',20],['Auta Nunes',12],['Quintino Cunha',15],['Barra do Ceará',25],['Genibaú',10],['Passaré',15]];
  let managedAreas=[];
  function bool(v,def=false){if(v===''||v==null)return def;return !['false','0','não','nao','inativo','off'].includes(String(v).toLowerCase())}
  function promoActive(p){if(!p.promocao||!Number(p.preco_promocional))return false;const now=new Date(),a=p.promo_inicio?new Date(p.promo_inicio):null,b=p.promo_fim?new Date(p.promo_fim):null;return(!a||isNaN(a)||now>=a)&&(!b||isNaN(b)||now<=b)}
  function mergedAreas(){const m=new Map(DEFAULT_DELIVERY_AREAS.map(([nome,fee])=>[nome.toLocaleLowerCase('pt-BR'),{nome,fee,ativo:true}]));managedAreas.forEach(a=>m.set(a.nome.toLocaleLowerCase('pt-BR'),a));return [...m.values()].filter(a=>a.ativo).sort((a,b)=>a.nome.localeCompare(b.nome,'pt-BR'));}
  function applyDeliveryAreas(){const sel=document.getElementById('deliveryArea');if(!sel)return;const current=sel.value;sel.innerHTML='<option value="">Selecione o bairro da entrega *</option>';mergedAreas().forEach(a=>{const o=document.createElement('option');o.value=a.nome;o.dataset.fee=String(a.fee);o.textContent=`${a.nome} — ${money(a.fee)}`;sel.appendChild(o)});if([...sel.options].some(o=>o.value===current))sel.value=current;sel.onchange=()=>{updateTotals();persistCustomer()};updateTotals();}
  normalizeProduct=function(p,i){const base=oldNormalize(p,i),normal=Number(p.preco??p.price??base.preco)||0,promo=Number(p.preco_promocional??p.preco_promo??0)||0;base.ativo=bool(p.ativo,true);base.preco_original=normal;base.promocao=bool(p.promocao,false);base.preco_promocional=promo;base.promo_inicio=p.promo_inicio||'';base.promo_fim=p.promo_fim||'';base.promocao_ativa=promoActive(base);if(base.promocao_ativa)base.preco=promo;return base;};
  fetchFreshProducts=async function(){if(cfg.ORDER_WEBAPP_URL){const u=new URL(cfg.ORDER_WEBAPP_URL);u.searchParams.set('action','products');u.searchParams.set('_t',Date.now());const ctrl=new AbortController(),timer=setTimeout(()=>ctrl.abort(),Number(cfg.DATA_TIMEOUT_MS)||7000);try{const res=await fetch(u,{cache:'no-store',signal:ctrl.signal,headers:{'Cache-Control':'no-cache, no-store, max-age=0','Pragma':'no-cache'}});if(!res.ok)throw new Error('HTTP '+res.status);const json=await res.json();if(!json.ok||!Array.isArray(json.products))throw new Error(json.error||'empty');managedAreas=json.products.filter(p=>String(p.categoria||'')===DELIVERY_CATEGORY).map(p=>({nome:String(p.nome||'').trim(),fee:Number(p.preco||0),ativo:bool(p.ativo,true)})).filter(a=>a.nome);applyDeliveryAreas();const all=json.products.filter(p=>String(p.categoria||'')!==DELIVERY_CATEGORY).map(normalizeProduct),data=all.filter(p=>p.ativo);localStorage.setItem('opeitica:lastProducts',JSON.stringify(data));localStorage.setItem('opeitica:lastSync',new Date().toISOString());return data;}finally{clearTimeout(timer)}}return oldFetch();};
  refreshProducts=async function(manual=false){if(!cfg.ORDER_WEBAPP_URL&&!cfg.SHEET_CSV_URL)return oldRefresh(manual);setSync('',manual?'Atualizando catálogo...':'Verificando catálogo...','Consultando preços, disponibilidade e entregas');try{const fresh=await fetchFreshProducts();state.products=fresh;syncCartPrices();buildTabs();renderProducts();setSync('ok','Catálogo atualizado',`Agora • ${new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}`);}catch(e){console.error('live-products refresh',e);setSync('error','Catálogo disponível','Não foi possível conferir a base agora');}};
  renderProducts=function(){oldRender();document.querySelectorAll('.product-card').forEach(card=>{const name=card.querySelector('h3')?.textContent||'',p=state.products.find(x=>x.nome===name);if(!p)return;card.classList.toggle('is-promo',!!p.promocao_ativa);const media=card.querySelector('.product-media');if(p.promocao_ativa&&media&&!media.querySelector('.promo-live-badge')){const b=document.createElement('span');b.className='promo-live-badge';b.textContent='🔥 OFERTA DO DIA';media.appendChild(b)}if(p.promocao_ativa){const row=card.querySelector('.price-row'),price=card.querySelector('.price');if(row&&price&&!row.querySelector('.old-price')){const old=document.createElement('del');old.className='old-price';old.textContent=money(p.preco_original);row.insertBefore(old,price)}}});};
  function startLive(){applyDeliveryAreas();setTimeout(()=>refreshProducts(false),250);setInterval(()=>{if(!document.hidden)refreshProducts(false)},5000);window.addEventListener('focus',()=>refreshProducts(false));document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshProducts(false)});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startLive,{once:true});else startLive();
})();